# Backfilled by the AppForge factory (P1-07). Fields marked null are not yet knowable —
# they depend on infrastructure (appforge-control, appforge-edge, Paperclip) not built yet.
product_id: json-workbench
name: JSON Workbench
description: "Local-first developer workbench for exploring and transforming large JSON data."
platform: [chrome]
state: PRODUCTION
portfolio_state: GROWTH
repository: ravitejakamalapuram/json-workbench
owner: founder
team: []
architecture: null
analytics_project: null # appforge-edge not yet provisioned (P1-10); docs/TELEMETRY.md notes telemetry is not enabled yet
store_ids:
  cws: higmlhleblpmdogjccpmjlnilpmhohal
monetization:
  model: free
permissions: .appforge/permissions.yaml
data_collection:
  collected: []
  stored:
    local: [chrome_storage_local_auto_render_flag, chrome_storage_session_pending_payload, localstorage_pipeline_and_theme]
    remote: []
  transmitted:
    endpoints: [] # the one fetch() call re-reads the active tab's own URL, not a third-party endpoint
  sensitive: [] # user JSON content is processed client-side (WASM: DuckDB-WASM, jq-web) and never leaves the browser
  third_party: []
  retention: { local: indefinite_until_user_clears, remote: none }
  deletion: "Uninstall clears all local/session storage and page localStorage for the extension"
security_classification: low
release_channel: default
current_version: "0.2.0"
kill_policy: null
scale_policy: null # this is the founder's current active growth-sprint priority (P4 in TASKS.md)
