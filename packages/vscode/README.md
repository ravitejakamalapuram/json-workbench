# JSON Workbench for VS Code

This package adds `JSON Workbench: Run Pipeline`. With a JSON document open,
invoke the command, paste a versioned Workbench pipeline, and the transformed
result opens in a new editor. It uses the same `@json-workbench/core` pipeline
contract as the Chrome extension and CLI; source data remains in the local
editor process.
